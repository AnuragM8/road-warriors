import React from "react";
import { Container, Typography } from "@mui/material";

const Home = () => (
  <Container>
    <Typography variant="h4" gutterBottom>Welcome to Our Driver Service</Typography>
    <Typography>Reliable and affordable driver services at your fingertips.</Typography>
  </Container>
);

export default Home;
